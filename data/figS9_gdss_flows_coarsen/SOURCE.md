# figS9_gdss_flows_coarsen: data

This figure re-quotients an existing input under 5 coarser equivalence
conventions (InChIKey, Murcko, Murcko-generic, formula, composition); it ships
no data of its own and reuses the **Fig 2 GDSS walk** under `data/fig2_flows/`.

## Analyze (default): replot, no GPU
```
bash repro/figS9_gdss_flows_coarsen.sh
```

## Re-create the data
Regenerate the reused input: see `data/fig2_flows/SOURCE.md`.
